const mongoose = require("mongoose")
const Schema = mongoose.Schema({
    tittle:{
        type:String,
    required:true,},

    desc: strings,
    url: Strings,
    username:string,
    image:string
})
const Project = new mongoose.model(mongoose)
module.exports = Project